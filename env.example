# =============================================================================
# env.example — Variáveis de ambiente do Catálogo Online
# Copie para .env.local e preencha com seus valores reais.
# NUNCA faça commit do arquivo .env.local!
# =============================================================================

# ── Supabase ──────────────────────────────────────────────────────────────────
# Obtenha em: https://app.supabase.com → seu projeto → Settings → API

# URL do projeto Supabase (ex.: https://xyzabcdef.supabase.co)
VITE_SUPABASE_URL=https://SEU_PROJETO.supabase.co

# Chave anônima pública (anon key) — segura para expor no frontend
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ── (Opcional) Service Role Key — use APENAS em scripts server-side/migrations
# NUNCA exponha essa chave no frontend!
# SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ── App ───────────────────────────────────────────────────────────────────────
# URL base do frontend (usado para redirects de auth)
VITE_APP_URL=http://localhost:3000
